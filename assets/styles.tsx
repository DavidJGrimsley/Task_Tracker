import { StyleSheet } from "react-native"

const globalStyles = StyleSheet.create({
  emptyText: {
    fontSize: 16,
    textAlign: 'center',
    backgroundColor: '#fff',
    fontStyle: 'italic',
    padding: 8,
  }
})

export default globalStyles